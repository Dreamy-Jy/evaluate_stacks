# API Design

I need to design success and failure output.

Redesign the database schema

Redesign Interface.
Start with SQL unit tests

- Use a better data model
- Better API Input
  - Handle input duplication: replace TreeSets(ordered) & HashSets(unordered) over Vecs.
  - Better Input Schema's: Using Enums for effect range
  - Output Schema's
- see how you can lean on Actix's abstractions for your

- I want the frontend to be unconcerned with IDs.


## Next Time

- Start with SQL unit test
- Auto generated docs
